dataset:
  dataset_name: 'movi_f'
  split: 'train'
  size: [512, 512]
  downsample_flow: 32
  max_kps: null
  dense_kps_use_all: true
  verbose: false
  debug: false
  normalize_images: true

  # MOVi-F-specific parameters
  # datapath points at the TFRecord shard directory (no extraction needed)
  datapath: '/home/slow1/Data/movi_f/512x512/1.0.0'
  kubric_dir: '/home/slow1/Projects/kubric'
  movi_f_config: '512x512'
  movi_f_shuffle_buffer: 64
  reverse_flow: false  # backward_flow is already target->source; no swap needed
