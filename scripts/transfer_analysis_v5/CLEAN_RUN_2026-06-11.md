# Clean Middlebury-free regeneration — 2026-06-11

Goal: recompute every fit/analysis table fresh from the **Middlebury-free** input,
archive the old CSVs for comparison, and remove the re-contamination trap so a
future re-run can't silently re-introduce Middlebury.

## What was done
1. **Verified** `transfer_table_nomid.csv` is *exactly* `transfer_table.csv` minus the
   162 Middlebury rows (same 82 cols, `DataFrame.equals` True after sort).
2. **Archived** all 42 result CSVs + the v4 core (`results_rule_v5core`) + the law
   REPORTs to `scripts/transfer_analysis_v5/_ARCHIVE_pre_clean_2026-06-11/`
   (+ `_manifest_md5.txt`). Nothing lost; diffable.
3. **Neutralized contamination at the source** (instead of editing 14 scripts, 4 of
   which hardcode the path): swapped the canonical table to the clean one.
   - `scripts/transfer_analysis_v3/transfer_table.csv` → now 1458 rows, 0 Middlebury.
   - contaminated original preserved as `transfer_table.csv.WITH_MIDDLEBURY_2026-06-11`.
   - Every producer (default `--table` *and* the hardcoded ones: per_regime_linear,
     consistency_analysis, l_directional_check, context_scale_calibration) now reads
     clean with zero logic edits.
4. **Re-ran** `run_v5.sh` (N_BOOT=200) end-to-end (EXIT_0) + a supplementary batch for
   the producers outside run_v5 (controls_fresh, ceiling_oracles, per_regime_linear,
   conditional_combination, loao_weight_transfer, policy_vs_fit_regret, joint_anchor,
   anticorr_by_benchmark, diagonal_sensitivity, regime_vs_level_deconfound).

## What changed (old archive → fresh clean)

**Bit-identical (already clean; reproduce exactly from the clean table — reassuring):**
Table 1 law REPORT.md (flip **+0.899**, p=0.0286, range [+0.807,+0.987]; catspp F/F
+0.527/+0.456, glunet T/T +0.763), CONSENSUS_RULE (0.733/0.751/0.713 cross-arch),
asym_vs_sym, rule_holdout_checks, ceiling_oracles, controls_fresh, selection_regret_rule,
intervention_oos, per_regime_linear_summary(+drop), per_regime_featureset_comparison(+drop),
**pairwise_gap_rule `acc`** (motion_rule >10 = 0.766/0.772/0.791 bit-identical; only the
pair-count `n` column differs), and all the new audit CSVs.

**Changed — exactly the files the audit flagged as contaminated, all moving the right way:**
| CSV | what moved | note |
|---|---|---|
| `loao_weight_transfer.csv` | n_ctx 20→18; pretrained **fit 0.252→0.311 (catspp), rule 0.440→0.507 (catspp), 0.669→0.718 (glunet)**; scratch ~unchanged | **quoted in main.tex ~l.644-648** ("+0.25/+0.46" → "+0.31/+0.47") |
| `conditional_combination.csv` | fit-free rows n_ctx 90→81; rule scratch 0.456→0.422, **pretrained 0.554→0.612** (now matches asym_vs_sym exactly) | clean rule pretrained 0.612 now ≥ OLS fit 0.593 — revisit "nothing beats the fit" |
| `policy_vs_fit_regret.csv` | n_ctx 90→81; pretrained median regret LOTO 2.97→7.90, JOINT 0.94→3.48 | not paper-quoted; informational |
| `joint_anchor_v2.csv` | 882→891 rows (more complete; reads nomid) | not a contamination change |

## STILL CONTAMINATED — cannot be regenerated (flagged)
- **`eps_rule_table.csv`** (fed tab_supp_eps): byte-identical to the Middlebury backup
  and has **no producer script** anywhere in the repo. **QUARANTINED** to
  `results/_QUARANTINE_contaminated_no_producer/eps_rule_table.csv.MIDDLEBURY_NO_PRODUCER`.
  Consequence: `tab_supp_eps` will now **fail loudly** rather than emit a contaminated
  table. To restore the eps-coverage replication, the per-direction ε-radius producer
  must be found or rewritten (it computes eps@1/4/16px directed coverage per variant).

## NOT regenerated this pass (deferred, with reason)
- **`decomp_*.csv` / `intervention_breakdown.csv`** — read peak-PCK from grid snapshots,
  not the table (so not Middlebury-contaminated via the table). The real fix is the
  **missing 4th pretrained 2×2 cell** (`kitti_badmotion_ft_gso_matte_pt1_fz1`), still
  training on GPU1. Regenerate decomp once it lands (and refresh intervention_breakdown,
  which currently has the stale TT n=3/4).
- **`benchsim_rule/`** (feeds F5) — `context_scale_calibration.py` is heavier and the
  benchsim_rule run was fit with Middlebury + an extra `raft|False|False` variant. Needs
  a dedicated rule/sym/policy re-run with the clean table; deferred to keep this pass fast.
- **`threetier_tier1.csv`, `sampling_stability.csv`** — orphan session artifacts, no
  producer; benchmark-agnostic; left as-is.

## Re-contamination is now blocked
Anyone can re-run `bash scripts/transfer_analysis_v5/run_v5.sh` and it stays clean,
because `transfer_table.csv` itself is Middlebury-free. **One remaining trap:** running
v3 `build_table.py` would rebuild `transfer_table.csv` *with* Middlebury from the feature
CSVs — if you ever do, immediately re-strip Middlebury (or keep using the nomid form).
